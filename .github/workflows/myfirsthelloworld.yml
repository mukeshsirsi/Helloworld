name: Hello World Demo

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: Amazon-Linux
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "Hello, GitHub Actions!"
